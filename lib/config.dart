// Centralized app configuration constants.
// Populate with your GitHub OAuth App Client ID for Device Flow.
const String GITHUB_CLIENT_ID = 'Ov23lisY1phNOytX9L6J';

// Scopes we request for GitHub OAuth Device Flow.
// Keep minimal; adjust if features require additional scopes.
const List<String> GITHUB_OAUTH_SCOPES = ['repo', 'read:user'];
